# ⚠️  DEPRECATED — This Express/PostgreSQL server is no longer deployed.
# The production backend is Cloudflare Workers + D1 (see workers/).
# This file is kept for reference only. Do NOT use this server.

# Server
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3001

# Database (PostgreSQL) — DEPRECATED, use D1 via workers/ instead
DATABASE_URL=postgresql://user:password@localhost:5432/lynetilt

# Legacy JSON Database (deprecated)
DATABASE_PATH=./data/lyne-tilt.db

# JWT (generate a secure random string for production)
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# CORS - comma-separated list of allowed origins for production
# In development, localhost ports are automatically allowed
CORS_ORIGINS=https://your-production-domain.com,https://admin.your-domain.com

# Initial Admin (used by seed script)
ADMIN_EMAIL=admin@lynetilt.com
ADMIN_PASSWORD=changeme123

# Stripe (get keys from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_your_test_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
