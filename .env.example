# Frontend Environment Variables

# API Base URL — In dev, Vite proxies /api to Workers automatically (see vite.config.ts)
# Only set this for production builds or to override the proxy target
# VITE_API_BASE=https://lyne-tilt-api.verdant-digital-co.workers.dev/api

# Stripe Public Key (starts with pk_)
VITE_STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key

# Google OAuth Client ID (for Google Sign-In)
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
